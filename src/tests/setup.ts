import { beforeAll, afterAll, afterEach } from 'vitest';

beforeAll(() => {
  // Setup mock browser APIs if needed
});

afterAll(() => {
  // Cleanup after all tests
});

afterEach(() => {
  // Reset the document body after each test
  document.body.innerHTML = '';
});

